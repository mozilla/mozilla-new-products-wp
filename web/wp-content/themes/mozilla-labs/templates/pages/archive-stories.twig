{% extends "layouts/base.twig" %}

{% block pageBody %}
  <header class="flex gap-2">
    <h1>
      {{ page.title }}/
    </h1>

    {% if page.description %}
    <p>{{ page.description }}</p>
    {% endif %}
  </header>
{% endblock %}
